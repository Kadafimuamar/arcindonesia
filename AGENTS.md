# AGENTS

- Gunakan TypeScript strict untuk seluruh kode.
- Pisahkan validasi, service, repository, dan UI.
- Seluruh mutasi sensitif harus divalidasi di server.
- Jangan menaruh secret di source code atau bundle browser.
- Pastikan `lint`, `typecheck`, `test`, dan `build` tetap hijau sebelum perubahan dianggap selesai.
